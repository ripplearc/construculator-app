/// Annotation to mark a class as a data model for linting and code generation purposes.
class DataModel {
  const DataModel();
}

/// Shorthand for the [DataModel] annotation.
const dataModel = DataModel(); 