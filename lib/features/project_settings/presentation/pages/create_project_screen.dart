import 'package:flutter/material.dart';

// TODO: [CA-174] Implement project creation form. https://ripplearc.youtrack.cloud/issue/CA-174
/// Stub screen for creating a new project (DASH-023). Replaced by the full
/// implementation in CA-174.
class CreateProjectScreen extends StatelessWidget {
  const CreateProjectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create a project')),
      body: const SizedBox.shrink(),
    );
  }
}
